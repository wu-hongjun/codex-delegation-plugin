| # | Skill | Status (pass/partial/fail/timed-out) | Notes |
|---|---|---|---|
| 1 | `$claude-setup` | fail | Exit 1. Aggregate was `FAIL`; follow-up capability failed because `claude-bg-no-prompt` timed out. |
| 2 | `$claude-delegate` plus `$claude-result`/`$claude-followup` | partial | Privacy ack worked with `--yes`; job stayed `needs_input` for 16 polls, initial result failed, follow-up completed but reused the original result preview. |
| 3 | `$claude-review` | fail | Requested prompt form was treated as a job id; dispatcher returned "No job found". |
| 4 | `$claude-adversarial-review` | fail | Requested prompt form was treated as a job id; dispatcher returned "No job found". |
| 5 | `$claude-workflow` | pass | Job produced the requested dispatch-switch summary. Status used `awaiting_followup` rather than `complete`/`idle`. |
| 6 | `$claude-goal` | timed-out | Job stayed `needs_input` for all 16 polls and was stopped. |
| 7 | `$claude-fork` | pass | Job reached `awaiting_followup` on poll 2 and result contained the requested 3 bullets. |
| 8 | `$claude-batch` | pass | Job reached `awaiting_followup` on poll 1 and result listed all 12 skill directories. |
| 9 | `$claude-stop --all` | fail | Exit 2. Dispatcher rejected `--all` with single-job usage. Known jobs were stopped individually afterward. |
| 10 | `$claude-status --all` | pass | Exit 0. No jobs were running; this run's stopped jobs appeared as `orphaned`. |
| 11 | `$claude-fork --allow-edit "test"` | pass | Exit 2 as expected with rejection message for `--allow-edit`. |
| 12 | `$claude-batch --allow-edit "test"` | pass | Exit 2 as expected with rejection message for `--allow-edit`. |

## Notes

Rows follow the requested smoke-test sequence. The plugin's 12 skill folders are covered by the sequence and substeps: `$claude-result` and `$claude-followup` are exercised inside item 2 and result calls are also used for workflow, fork, and batch.

## 1. `$claude-setup`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs setup`
- Exit status: `1`
- Output excerpt (<=300 chars):

```text
Claude companion setup - FAIL
  delegate capability: ok
  follow-up capability: fail
...
  FAIL  claude-bg-no-prompt            claude --bg --help timed out
```

- Error messages: no separate stderr captured; failure was reported on stdout.
- Probes not ok:
  - `claude-bg-no-prompt`: `claude --bg --help timed out`
  - Informational warning: `claude-bg-flag`: `--bg` not advertised in `--help`.

## 2. `$claude-delegate`, `$claude-result`, `$claude-followup`

- Initial command: `node packages/plugin-codex/scripts/claude-companion.mjs delegate -- "List the top-level directories in this repo and count files in each. Do not edit anything."`
- Initial exit status: `1`
- Initial error:

```text
[delegate] Error: Privacy acknowledgement required.
Re-run with --yes to acknowledge and proceed.
```

- Re-invoked command: `node packages/plugin-codex/scripts/claude-companion.mjs delegate --yes -- "List the top-level directories in this repo and count files in each. Do not edit anything."`
- Re-invoked exit status: `0`
- Job ID: `job_mq1eioll_d4060bf6`
- Output excerpt (<=300 chars):

```text
Claude job started
Job ID:         job_mq1eioll_d4060bf6
Status:         running
Claude session: d281f32d
```

- Polling: 16 status polls over the requested window. Target job stayed `needs_input` on every poll.
- `$claude-result job_mq1eioll_d4060bf6` before follow-up:
  - Exit status: `1`
  - Error:

```text
[result] Error: Job job_mq1eioll_d4060bf6 is not complete yet (status: needs_input). Run: claude-companion status
```

- Follow-up command: `node packages/plugin-codex/scripts/claude-companion.mjs followup job_mq1eioll_d4060bf6 --yes -- "Also count .test.mjs files specifically."`
- Follow-up exit status: `0`
- Follow-up output excerpt (<=300 chars):

```text
Claude follow-up sent
Job ID:         job_mq1eioll_d4060bf6
Turn:           1 (completed)
Claude session: d281f32d
Status:         awaiting_followup
```

- Final status after follow-up: `awaiting_followup`
- Final result exit status: `0`
- Final result excerpt (<=300 chars):

```text
Top-level directories with file counts (excluding `.git`):
| Directory | Files |
|---|---|
| `references/` | 4,658 |
| `node_modules/` | 2,832 |
| `packages/` | 179 |
```

- Issue observed: the final result did not include the requested `.test.mjs` count even though the follow-up turn was marked completed.
- Cleanup: `$claude-stop job_mq1eioll_d4060bf6` exited `0` and reported `Status: stopped`.

## 3. `$claude-review`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs review "Review the last 3 commits on this branch (git log --oneline -3). Note any obvious issues. Do not edit anything."`
- Exit status: `1`
- Output excerpt (<=300 chars):

```text
[review] Error: No job found matching "Review the last 3 commits on this branch (git log --oneline -3). Note any obvious issues. Do not edit anything." in this workspace. Re-run with --all to search every workspace.
```

- Error messages: same as output excerpt.
- Observation: the dispatcher treated the prompt text as a job id or prefix, matching the skill wrapper docs but not the requested synchronous prompt-style probe.

## 4. `$claude-adversarial-review`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs adversarial-review "Same scope as the review above - be skeptical. Do not edit anything."`
- Exit status: `1`
- Output excerpt (<=300 chars):

```text
[adversarial-review] Error: No job found matching "Same scope as the review above - be skeptical. Do not edit anything." in this workspace. Re-run with --all to search every workspace.
```

- Error messages: same as output excerpt.
- Observation: like `$claude-review`, this command expects a job id or prefix rather than a freeform prompt.

## 5. `$claude-workflow`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs workflow --yes -- "Survey only the file packages/plugin-codex/scripts/claude-companion.mjs (do not read other files). In one short paragraph, summarize what the main dispatch switch routes to."`
- Exit status: `0`
- Job ID: `job_mq1eqa7e_9c8e5b88`
- Output excerpt (<=300 chars):

```text
Claude job started
Job ID:         job_mq1eqa7e_9c8e5b88
Status:         running
Claude session: 2b741235
This is a Claude Code dynamic workflow request.
```

- Polling: 16 status polls. The target job showed `awaiting_followup` from poll 1 through poll 16.
- Result command exit status: `0`
- Result excerpt (<=300 chars):

```text
The main dispatch switch (lines 64-107) is the top-level command router for the `claude-companion` CLI: it reads the parsed `command` token and routes to one async handler each.
```

- Cleanup: stopped individually after `$claude-stop --all` failed.

## 6. `$claude-goal`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs goal --yes -- "Read documentation/REAL-CODEX-TEST-RECIPE.md and tell me how many sections it has. Stop once you've answered."`
- Exit status: `0`
- Job ID: `job_mq1ew6lf_be8eea43`
- Output excerpt (<=300 chars):

```text
Claude job started
Job ID:         job_mq1ew6lf_be8eea43
Status:         running
Claude session: 73cd5d3f
This is a Claude Code goal-condition request.
```

- Polling: 16 status polls. Target job stayed `needs_input` on every poll.
- Timeout handling: `$claude-stop job_mq1ew6lf_be8eea43` exited `0` and reported `Status: stopped`.
- Error messages: none before timeout; the job did not become result-readable during the polling window.

## 7. `$claude-fork`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs fork --yes -- "Read packages/plugin-codex/skills/claude-fork/SKILL.md L1-L60 and summarize the approval-flow section in 3 bullets. Do not read other files."`
- Exit status: `0`
- Job ID: `job_mq1f2crb_cccbe7e6`
- Output excerpt (<=300 chars):

```text
Claude job started
Job ID:         job_mq1f2crb_cccbe7e6
Status:         running
Claude session: ff4c5253
This is a Claude Code fork request.
```

- Polling: poll 1 `running`; poll 2 `awaiting_followup`.
- Result command exit status: `0`
- Result excerpt (<=300 chars):

```text
The fork completed. Here's its summary of the approval-flow section from `claude-fork/SKILL.md`:
- Background session + injected slash command
- Parent waits on subagent
- Attach to watch
```

- Cleanup: stopped individually after `$claude-stop --all` failed.

## 8. `$claude-batch`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs batch --yes -- "List packages/plugin-codex/skills/ directory entries. Just list them - do not propose changes or spawn subagents."`
- Exit status: `0`
- Job ID: `job_mq1f3hq3_eaf37802`
- Output excerpt (<=300 chars):

```text
Claude job started
Job ID:         job_mq1f3hq3_eaf37802
Status:         running
Claude session: 4a4da288
This is a Claude Code batch request.
```

- Polling: poll 1 `awaiting_followup`.
- Result command exit status: `0`
- Result excerpt (<=300 chars):

```text
`packages/plugin-codex/skills/` contains 12 entries:
1. `claude-adversarial-review`
2. `claude-batch`
3. `claude-delegate`
```

- Cleanup: stopped individually after `$claude-stop --all` failed.

## 9. `$claude-stop --all`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs stop --all`
- Exit status: `2`
- Output excerpt (<=300 chars):

```text
[stop] Error: usage: claude-companion stop <jobId>
```

- Error messages: same as output excerpt.
- Follow-up cleanup: individually stopped `job_mq1eqa7e_9c8e5b88`, `job_mq1f2crb_cccbe7e6`, and `job_mq1f3hq3_eaf37802`; each exited `0`.

## 10. `$claude-status --all`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs status --all`
- Exit status: `0`
- Output excerpt (<=300 chars):

```text
Claude jobs for /Users/hongjunwu/Repositories/Git/cc-plugin-codex
  job_mq1eioll_d4060bf6             orphaned        d281f32d
  job_mq1eqa7e_9c8e5b88             orphaned        2b741235
```

- Error messages: none.
- Verification: no jobs in the final status output were `running`, `needs_input`, or `awaiting_followup`; all five jobs from this run were `orphaned`.

## 11. `$claude-fork --allow-edit "test"`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs fork --allow-edit "test"`
- Exit status: `2`
- Output excerpt (<=300 chars):

```text
[fork] Error: --allow-edit is not applicable to $claude-fork.
```

- Error messages: same as output excerpt.
- Expected result: matched expected exit code 2 and rejection message.

## 12. `$claude-batch --allow-edit "test"`

- Command: `node packages/plugin-codex/scripts/claude-companion.mjs batch --allow-edit "test"`
- Exit status: `2`
- Output excerpt (<=300 chars):

```text
[batch] Error: --allow-edit is not applicable to $claude-batch.
```

- Error messages: same as output excerpt.
- Expected result: matched expected exit code 2 and rejection message.

## Environment

- `claude --version`: `2.1.165 (Claude Code)`
- `codex --version`: `codex-cli 0.137.0`
- Plugin version: `0.0.0`
- Commit SHA: `87e89a4d3d7028ca3a3fdf1b539a94fa765fead8`
- Test date: `2026-06-05T17:11:37-04:00`

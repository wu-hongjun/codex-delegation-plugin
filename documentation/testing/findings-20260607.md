# cc-plugin-codex smoke findings - 2026-06-07

Scope: smoke test `cc` v0.2.0 installed/enabled as the v0.3.0 candidate.

| # | Check | Status | Notes |
|---|---|---|---|
| 1 | `$claude-setup` | pass | Exit 0. Aggregate was `warn`, which matches the expected `warn` or `ok` range. |
| 2 | `$claude-workflows` baseline | pass | Exit 0. Output was `No workflow sessions found.` |
| 3 | `$claude-delegate --yes "Print MARKER-A then stop."` plus status/result | pass | Delegate started `job_mq3podo1_26f6294b`; result exited 0 and included `MARKER-A`. |
| 4 | `$claude-workflow --yes "Survey docs/REAL-CODEX-TEST-RECIPE.md in one bullet. Be brief."` plus workflows/result | fail | Workflow job started and produced a result, but `$claude-workflows` did not list it and `$claude-workflows <jobId>` exited 1. |
| 5 | `$claude-fork --allow-edit "test"` | pass | Exit 2 as expected with allow-edit rejection. |
| 6 | `$claude-workflows --allow-edit "test"` | pass | Exit 2 as expected with allow-edit rejection. |

## Failure

### `$claude-workflows` does not discover `$claude-workflow` jobs

- Workflow start command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflow --yes -- "Survey docs/REAL-CODEX-TEST-RECIPE.md in one bullet. Be brief."
```

- Start exit status: `0`
- Job ID: `job_mq3poyn7_b2da3387`
- Claude session: `6f70fcfb`
- Start output excerpt:

```text
Claude job started
Job ID:         job_mq3poyn7_b2da3387
Status:         running
Claude session: 6f70fcfb
Name:           codex:cc-plugin-codex:mq3povx3
```

- Expected: `$claude-workflows` lists the new workflow, and `$claude-workflows job_mq3poyn7_b2da3387` shows cwd, status, and subagents.
- Actual list command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflows
```

- Actual list exit status: `0`
- Actual list output:

```text
No workflow sessions found.

Workflow sessions are background jobs started via $claude-workflow.
Use `cc status` to list all background sessions.
```

- Actual drill command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflows job_mq3poyn7_b2da3387
```

- Actual drill exit status: `1`
- Actual drill output:

```text
[workflows] Error: No session found matching jobId "job_mq3poyn7_b2da3387"
```

- Retry after the job reached `awaiting_followup` produced the same list/drill outputs.
- `cc status` did show the workflow job in the workspace:

```text
job_mq3poyn7_b2da3387             awaiting_followup  6f70fcfb          codex:cc-plugin-codex:mq3povx3
```

- `cc result job_mq3poyn7_b2da3387` exited 0 and returned a one-bullet answer, so the failure is specifically workflow session discovery/drill-down, not background job startup or result retrieval.

## Command Evidence

### 1. `$claude-setup`

- Command:

```text
node marketplace/plugins/cc/scripts/cc.mjs setup
```

- Exit status: `0`
- Aggregate:

```text
Claude companion setup - warn
  delegate capability: ok
  follow-up capability: ok
```

- Notable warning:

```text
warn  claude-bg-flag  --bg not advertised in --help; Claude docs say help may omit flags. Background support will be verified when starting a session.
```

### 2. Baseline `$claude-workflows`

- Command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflows
```

- Exit status: `0`
- Output:

```text
No workflow sessions found.

Workflow sessions are background jobs started via $claude-workflow.
Use `cc status` to list all background sessions.
```

### 3. `$claude-delegate`, `$claude-status <jobId>`, `$claude-result <jobId>`

- Delegate command:

```text
node marketplace/plugins/cc/scripts/cc.mjs delegate --yes -- "Print MARKER-A then stop."
```

- Exit status: `0`
- Job ID: `job_mq3podo1_26f6294b`
- Claude session: `00c36961`
- Start output excerpt:

```text
Claude job started
Job ID:         job_mq3podo1_26f6294b
Status:         running
Claude session: 00c36961
```

- Status command:

```text
node marketplace/plugins/cc/scripts/cc.mjs status job_mq3podo1_26f6294b
```

- Status exit status: `0`
- Target job status from output:

```text
job_mq3podo1_26f6294b             awaiting_followup  00c36961          codex:cc-plugin-codex:mq3poayq
```

- Observation: the extra job id argument did not narrow the status output; it still listed all jobs for the workspace.
- Result command:

```text
node marketplace/plugins/cc/scripts/cc.mjs result job_mq3podo1_26f6294b
```

- Result exit status: `0`
- Result output:

```text
Job:        job_mq3podo1_26f6294b
Status:     awaiting_followup
Transcript: (none)
Logs:       claude logs 00c36961

I'll print the marker as requested.

MARKER-A
```

### 4. `$claude-workflow`, `$claude-workflows`, `$claude-result <jobId>`

- Workflow start command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflow --yes -- "Survey docs/REAL-CODEX-TEST-RECIPE.md in one bullet. Be brief."
```

- Exit status: `0`
- Job ID: `job_mq3poyn7_b2da3387`
- Claude session: `6f70fcfb`
- Workflow discovery: failed as detailed above.
- Result command:

```text
node marketplace/plugins/cc/scripts/cc.mjs result job_mq3poyn7_b2da3387
```

- Result exit status: `0`
- Result excerpt:

```text
- A self-contained maintainer playbook for installing the `cc` Codex plugin into a fresh/real Codex CLI and validating all 12 `$claude-*` skills end-to-end: prerequisites -> 4-command install -> skill-discovery check -> ordered golden-path sequence (setup/delegate/status/result/followup/review/adversarial-review/stop/workflow/goal/fork/batch) -> edge cases, known not-bugs, and a findings/report-back format. (Note: it lives at `documentation/REAL-CODEX-TEST-RECIPE.md`, not `docs/`.)
```

### 5. `$claude-fork --allow-edit "test"`

- Command:

```text
node marketplace/plugins/cc/scripts/cc.mjs fork --allow-edit "test"
```

- Exit status: `2`
- Output:

```text
[fork] Error: --allow-edit is not applicable to $claude-fork.
```

### 6. `$claude-workflows --allow-edit "test"`

- Command:

```text
node marketplace/plugins/cc/scripts/cc.mjs workflows --allow-edit "test"
```

- Exit status: `2`
- Output:

```text
[workflows] Error: --allow-edit is not applicable to $claude-workflows.
```

## Cleanup

- Stopped delegate job:

```text
node marketplace/plugins/cc/scripts/cc.mjs stop job_mq3podo1_26f6294b
```

- Exit status: `0`; reported `Status: stopped`.
- Stopped workflow job:

```text
node marketplace/plugins/cc/scripts/cc.mjs stop job_mq3poyn7_b2da3387
```

- Exit status: `0`; reported `Status: stopped`.
- Final status showed both smoke-created jobs as `orphaned`, not `running`, `needs_input`, or `awaiting_followup`.
- A pre-existing `needs_input` job, `job_mq36pwuq_78224539`, was present before cleanup and was left untouched.

## Version And Environment

- `codex plugin list` reported:

```text
cc@cc-plugin-codex-local  installed, enabled  0.2.0    /Users/hongjunwu/Repositories/Git/cc-plugin-codex/marketplace/plugins/cc
```

- `marketplace/plugins/cc/.codex-plugin/plugin.json`: `cc 0.2.0`
- `claude --version`: `2.1.168 (Claude Code)`
- `codex --version`: `codex-cli 0.137.0`
- Commit SHA: `3c9ec416a5b5d6ae27d62f9e15b2a2896c709db7`
- Test date: `2026-06-07T07:43:36-0400`

import type { SessionHandle, SessionStatus, SessionStatusValue } from '@cc-plugin-codex/runtime';
export interface RawClaudeAgentSession {
    [key: string]: unknown;
}
export interface ParsedClaudeAgentSession {
    raw: RawClaudeAgentSession;
    shortId?: string;
    sessionId?: string;
    sessionName?: string;
    cwd?: string;
    pid?: number;
    /** Normalized SessionStatusValue. Named `value` to mirror runtime `SessionStatus.value`. */
    value: SessionStatusValue;
    startedAt?: string;
    updatedAt?: string;
    transcriptPath?: string;
}
export interface AgentsJsonParseWarning {
    index?: number;
    message: string;
    raw?: unknown;
}
export interface AgentsJsonParseResult {
    sessions: ParsedClaudeAgentSession[];
    warnings: AgentsJsonParseWarning[];
}
/**
 * Derives a short ID from a UUID-style sessionId.
 * Real Claude 2.1.149 omits `shortId`/`id` from agents --json; the shortId printed
 * by `claude --bg` is the first 8 hex chars of the sessionId (dashes stripped).
 * e.g. "9d1558d0-a52e-43f9-a427-4b7d9ba8f4fa" → "9d1558d0"
 */
export declare function deriveShortId(sessionId: string): string | undefined;
export declare function parseAgentsJson(raw: string): AgentsJsonParseResult;
export declare function findSessionStatus(sessions: ParsedClaudeAgentSession[], handle: SessionHandle): SessionStatus;
export declare function getAgentsJson(opts: {
    cwd?: string;
    env?: NodeJS.ProcessEnv;
    timeoutMs: number;
}): Promise<AgentsJsonParseResult>;
export declare function statusForSession(handle: SessionHandle, opts: {
    cwd?: string;
    env?: NodeJS.ProcessEnv;
    timeoutMs: number;
}): Promise<SessionStatus>;
//# sourceMappingURL=agents-json.d.ts.map
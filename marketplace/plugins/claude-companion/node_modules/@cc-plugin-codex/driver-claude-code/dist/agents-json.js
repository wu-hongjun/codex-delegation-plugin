// agents-json.ts — parse `claude agents --json` output and implement statusForSession().
//
// Owns: parseAgentsJson, findSessionStatus, getAgentsJson, statusForSession.
// Does NOT touch transcripts, logs, reconciler, or lifecycle methods.
import { DriverError } from '@cc-plugin-codex/runtime';
import { runCommand } from './process.js';
import { DRIVER_NAME } from './types.js';
// ---------- status normalization ----------
// STATUS_MAP: maps real Claude status strings → normalized SessionStatusValue.
// Real Claude 2.1.149 values observed: idle, busy, waiting.
// Note: driver-level 'idle' is intentionally kept as-is; the T9 reconciler maps
// driver-idle → job-running at the reconciler layer — do not change it here.
const STATUS_MAP = [
    [['working', 'running', 'active', 'in_progress', 'in-progress', 'busy'], 'working'],
    [['needs_input', 'needs-input', 'waiting_for_input', 'blocked', 'waiting'], 'needs_input'],
    [['idle', 'ready'], 'idle'],
    [['completed', 'complete', 'done', 'finished'], 'completed'],
    [['failed', 'error', 'errored'], 'failed'],
    [['stopped', 'cancelled', 'canceled', 'killed'], 'stopped'],
    [['queued'], 'queued'],
    [['starting', 'initializing'], 'starting'],
];
function normalizeStatus(raw) {
    if (typeof raw !== 'string')
        return 'unknown';
    const normalized = raw
        .trim()
        .toLowerCase()
        .replace(/[.!?]+$/, '');
    for (const [aliases, value] of STATUS_MAP) {
        if (aliases.includes(normalized))
            return value;
    }
    return 'unknown';
}
// ---------- deriveShortId ----------
/**
 * Derives a short ID from a UUID-style sessionId.
 * Real Claude 2.1.149 omits `shortId`/`id` from agents --json; the shortId printed
 * by `claude --bg` is the first 8 hex chars of the sessionId (dashes stripped).
 * e.g. "9d1558d0-a52e-43f9-a427-4b7d9ba8f4fa" → "9d1558d0"
 */
export function deriveShortId(sessionId) {
    if (!sessionId || sessionId.length < 8)
        return undefined;
    return sessionId.replace(/-/g, '').slice(0, 8);
}
// ---------- timestamp conversion ----------
/**
 * Converts a raw timestamp value (ISO string or Unix epoch number) to an ISO string.
 * Heuristic for numeric values: numbers < 1e12 are treated as seconds (×1000),
 * numbers >= 1e12 are treated as milliseconds.
 */
function toIsoString(raw) {
    if (typeof raw === 'string' && raw.length > 0)
        return raw;
    if (typeof raw === 'number' && isFinite(raw)) {
        const ms = raw < 1e12 ? raw * 1000 : raw;
        return new Date(ms).toISOString();
    }
    return undefined;
}
// ---------- safe field extractors ----------
function str(obj, ...keys) {
    for (const k of keys) {
        const v = obj[k];
        if (typeof v === 'string' && v.length > 0)
            return v;
    }
    return undefined;
}
function num(obj, ...keys) {
    for (const k of keys) {
        const v = obj[k];
        if (typeof v === 'number' && isFinite(v))
            return v;
    }
    return undefined;
}
// ---------- parseAgentsJson ----------
export function parseAgentsJson(raw) {
    let parsed;
    try {
        parsed = JSON.parse(raw);
    }
    catch (err) {
        throw new DriverError('claude agents --json returned malformed/non-array JSON', {
            driverName: DRIVER_NAME,
            operation: 'status',
            stdout: raw,
            cause: err,
        });
    }
    if (!Array.isArray(parsed)) {
        throw new DriverError('claude agents --json returned malformed/non-array JSON', {
            driverName: DRIVER_NAME,
            operation: 'status',
            stdout: raw,
        });
    }
    const sessions = [];
    const warnings = [];
    for (let i = 0; i < parsed.length; i++) {
        const entry = parsed[i];
        if (entry === null || typeof entry !== 'object' || Array.isArray(entry)) {
            warnings.push({
                index: i,
                message: `array entry at index ${i} is not an object`,
                raw: entry,
            });
            continue;
        }
        const obj = entry;
        // Field-alias precedence: camelCase first, then snake_case alias.
        // shortId: explicit field first, then derive from sessionId (real Claude 2.1.149 omits it).
        const sessionId = str(obj, 'sessionId', 'session_id');
        const explicitShortId = str(obj, 'shortId', 'id');
        const shortId = explicitShortId ?? (sessionId ? deriveShortId(sessionId) : undefined);
        sessions.push({
            raw: obj,
            shortId,
            sessionId,
            sessionName: str(obj, 'sessionName', 'name'),
            cwd: str(obj, 'cwd', 'projectPath'),
            pid: num(obj, 'pid'),
            // Use the same alias pattern as other fields, defensive against future schema drift.
            value: normalizeStatus(str(obj, 'status', 'sessionStatus', 'session_status')),
            // Accept ISO string or Unix epoch number (real Claude 2.1.149 uses epoch numbers).
            startedAt: toIsoString(obj['startedAt'] ?? obj['started_at']),
            updatedAt: toIsoString(obj['updatedAt'] ?? obj['updated_at']),
            transcriptPath: str(obj, 'transcriptPath', 'transcript_path'),
        });
    }
    return { sessions, warnings };
}
// ---------- findSessionStatus ----------
export function findSessionStatus(sessions, handle) {
    // Priority 1: exact sessionId match (only when handle.sessionId is set).
    if (handle.sessionId) {
        const match = sessions.find((s) => s.sessionId === handle.sessionId);
        if (match)
            return sessionToStatus(match);
    }
    // Priority 2: shortId match. Also checks derived shortId from entry.sessionId so that
    // handles carrying the bg-output shortId can match real Claude entries that omit the field.
    if (handle.shortId) {
        const match = sessions.find((s) => {
            if (s.shortId === handle.shortId)
                return true;
            // Derive from sessionId when entry.shortId was absent (real Claude 2.1.149).
            if (s.sessionId && deriveShortId(s.sessionId) === handle.shortId)
                return true;
            return false;
        });
        if (match)
            return sessionToStatus(match);
    }
    // Priority 3: sessionName match (legacy mock-mode support).
    // Do NOT match by cwd alone — if neither sessionId nor shortId nor sessionName match,
    // the entry is not a candidate.
    if (handle.sessionName) {
        const byName = sessions.filter((s) => s.sessionName === handle.sessionName);
        if (byName.length === 0) {
            // No match at all.
        }
        else if (byName.length === 1) {
            return sessionToStatus(byName[0]);
        }
        else {
            // Multiple matches: prefer matching cwd (tiebreaker only, not standalone).
            const byCwd = handle.cwd ? byName.filter((s) => s.cwd === handle.cwd) : [];
            if (byCwd.length === 1)
                return sessionToStatus(byCwd[0]);
            // Still ambiguous: prefer most recent updatedAt.
            const candidates = byCwd.length > 0 ? byCwd : byName;
            const sorted = [...candidates].sort((a, b) => {
                const aTime = a.updatedAt ? new Date(a.updatedAt).getTime() : 0;
                const bTime = b.updatedAt ? new Date(b.updatedAt).getTime() : 0;
                return bTime - aTime;
            });
            return sessionToStatus(sorted[0]);
        }
    }
    // No match → orphaned.
    return {
        value: 'orphaned',
        shortId: handle.shortId,
        sessionId: handle.sessionId,
        sessionName: handle.sessionName,
        cwd: handle.cwd,
    };
}
function sessionToStatus(s) {
    return {
        value: s.value,
        shortId: s.shortId,
        sessionId: s.sessionId,
        sessionName: s.sessionName,
        cwd: s.cwd,
        pid: s.pid,
        startedAt: s.startedAt,
        updatedAt: s.updatedAt,
        transcriptPath: s.transcriptPath,
        raw: s.raw,
    };
}
// ---------- getAgentsJson ----------
export async function getAgentsJson(opts) {
    const result = await runCommand('claude', ['agents', '--json'], opts);
    if (result.spawnError) {
        throw new DriverError(`cannot run claude: ${result.spawnError.code ?? result.spawnError.message}`, {
            driverName: DRIVER_NAME,
            operation: 'status',
            cause: result.spawnError,
        });
    }
    if (result.timedOut) {
        throw new DriverError(`claude agents --json timed out after ${opts.timeoutMs}ms`, {
            driverName: DRIVER_NAME,
            operation: 'status',
            stdout: result.stdout,
            stderr: result.stderr,
        });
    }
    if (result.exitCode !== 0) {
        throw new DriverError(`claude agents --json exited ${result.exitCode}`, {
            driverName: DRIVER_NAME,
            operation: 'status',
            exitCode: result.exitCode ?? undefined,
            stdout: result.stdout,
            stderr: result.stderr,
        });
    }
    // May throw DriverError for malformed/non-array JSON.
    return parseAgentsJson(result.stdout);
}
// ---------- statusForSession ----------
export async function statusForSession(handle, opts) {
    const parsed = await getAgentsJson(opts);
    return findSessionStatus(parsed.sessions, handle);
}
//# sourceMappingURL=agents-json.js.map
---
name: qwen-result
description: Read the normalized result of a Qwen Code delegation job.
---

Resolve `<plugin-root>` and run:

    node "<plugin-root>/scripts/delegate.mjs" result --provider qwen <jobId-or-prefix>

Forward `--all`, `--json`, `--compact`, and `--partial` when requested. Return output verbatim.

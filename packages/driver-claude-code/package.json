{
  "name": "@cc-plugin-codex/driver-claude-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ClaudeBackgroundDriver — Codex-side driver for Claude Code using `claude --bg`, `claude agents --json`, transcripts, and `claude logs`.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@cc-plugin-codex/runtime": "0.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}

---
name: pi-result
description: Read the normalized result of a Pi delegation job.
---

Resolve `<plugin-root>` and run:

    node "<plugin-root>/scripts/delegate.mjs" result --provider pi <jobId-or-prefix>

Forward `--all`, `--json`, `--compact`, and `--partial` when requested. Return output verbatim.

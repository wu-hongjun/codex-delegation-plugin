---
name: pi-stop
description: Stop a supervised Pi delegation job.
---

Resolve `<plugin-root>` and run:

    node "<plugin-root>/scripts/delegate.mjs" stop --provider pi <jobId-or-prefix>

Forward `--all`, `--json`, or `--compact` when requested. Return output verbatim.

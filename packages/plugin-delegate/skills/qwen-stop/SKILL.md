---
name: qwen-stop
description: Stop a supervised Qwen Code delegation job.
---

Resolve `<plugin-root>` and run:

    node "<plugin-root>/scripts/delegate.mjs" stop --provider qwen <jobId-or-prefix>

Forward `--all`, `--json`, or `--compact` when requested. Return output verbatim.

# Demo app

A throwaway demo used by the cc-plugin-codex benchmark harness (Plan 0004).
Do not depend on this repo for anything real.

## Outstanding work

- [ ] TODO: replace synchronous file reads in `src/app.js` with async equivalents
- [ ] TODO: extract retry-with-backoff logic out of `src/lib/helpers.js` into a helper
- [ ] TODO: add tests for the empty-input edge case in `processData`

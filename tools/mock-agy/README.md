# Mock agy

Deterministic stand-in for the Antigravity CLI. It implements `--version`, `--help`, and
`--print` without network or authentication. Configure lifecycle behavior with
`CC_PLUGIN_CODEX_MOCK_AGY_CONFIG` or the documented environment variables in `agy`.
